import { ChangesBackgroundColorDirective } from './changes-background-color.directive';

describe('ChangesBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangesBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
